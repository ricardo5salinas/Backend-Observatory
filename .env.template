
# -----------------------------------------------------------------------------
# Environment template for Backend-Observatory
# -----------------------------------------------------------------------------
# Copy this file to `.env` and fill in the values. Keep secrets out of version
# control (add `.env` to .gitignore). Use the `DATABASE_URL` variable when
# possible (preferred for cloud providers such as Neon). All variables below are
# optional if you provide a valid `DATABASE_URL`.
# -----------------------------------------------------------------------------

# Database settings
# Preferred: set the full connection string in DATABASE_URL.
# Example (sanitized Neon example):
# DATABASE_URL='postgresql://<user>:<password>@<host>/<database>?sslmode=require&channel_binding=require'
DATABASE_URL=

# Optional per-value fallbacks (used when DATABASE_URL is not set)
DB_USER=postgres
DB_HOST=localhost
DB_PASSWORD=your_db_password
DB_NAME=Observatory
DB_PORT=5432
# Optional: set to true to ping the database when the app imports the pool.
DB_CHECK_ON_START=false

# JWT / Auth
# Note: set a long, random secret in production (use a secret manager if possible)
JWT_SECRET=change_this_secret

# Application settings
# NODE_ENV: 'development' | 'production' | 'test'
NODE_ENV=development
PORT=3000

# -----------------------------------------------------------------------------
# Security & Usage tips:
# - Never commit real secrets to git. Use environment variables, a vault, or
#   your cloud provider's secret manager for production secrets.
# - If you use a hosted Postgres (Neon/Heroku), prefer DATABASE_URL with
#   SSL parameters (e.g., '?sslmode=require').
# - When running locally, you can set variables in PowerShell:
#     $env:DATABASE_URL='postgresql://user:pass@host/db?sslmode=require'
#   Or create a local `.env` file and run `npm run dev` (with dotenv installed).
# -----------------------------------------------------------------------------
